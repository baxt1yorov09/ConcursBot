version: '3.8'

services:
  bot:
    build: .
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./bot.db:/app/bot.db
      - ./.env:/app/.env
    networks:
      - botnet

networks:
  botnet:
    driver: bridge
